import express, { Router } from 'express';
//import { getComplexReport } from '../controllers/reportController';

const router: Router = express.Router();

//router.get('/complex', getComplexReport);

export = router;
